package com.iti.core.model

/**
 * Example only — shows the shape a shared model takes in this module
 * (plain data class, no persistence annotations, no Android imports).
 * Delete this once the first real shared model (e.g. User, Company) is added.
 */
data class ExampleModel(
    val id: String,
    val name: String,
)
